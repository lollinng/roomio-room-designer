/**
 * Agent H — Photo-Texture Mapping. Public barrel for the drop-in library.
 * The pipeline (T1 detect+crop, T2 texture-ify, T3 apply) lands across milestones H1–H5;
 * see PROGRESS.md. For now this exports the published contract.
 */
export * from './contract'
